# 在线课程系统 - 毕业设计答辩材料

## 一、项目概述

**项目名称**：医护培训在线课程平台
**技术架构**：前后端分离

| 组成部分 | 技术栈 |
|---------|--------|
| 后端 | Spring Boot + MyBatis-Plus |
| 前端 | Next.js 14 + TypeScript + Tailwind CSS |
| 数据库 | MySQL 8.0 |
| 安全 | JWT + Spring Security |
| 支付 | 支付宝当面付 + 微信支付Native |

---

## 二、系统架构设计

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端 (Next.js)                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  首页    │  │ 课程列表  │  │ 视频播放  │  │ 个人中心  │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    AuthContext (状态管理)                 │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │ HTTP/REST API
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      后端 (Spring Boot)                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              JwtAuthenticationFilter (JWT认证)           │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              RoleCheckAspect (AOP角色检查)                │   │
│  └──────────────────────────────────────────────────────────┘   │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │AuthController│ │VideosController│ │PaymentController│ │AdminController│ │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         MySQL Database                           │
│  users | videos | payments | watch_history | comments | ...      │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心功能模块

```
在线课程系统
├── 认证模块 (Authentication)
│   ├── 用户注册
│   ├── 用户登录
│   └── JWT Token 管理
├── 视频模块 (Videos)
│   ├── 视频列表/搜索
│   ├── 视频详情/播放
│   ├── 热门推荐
│   └── 访问权限控制
├── 会员模块 (Membership)
│   ├── 会员开通
│   ├── 会员状态查询
│   └── 权限校验
├── 支付模块 (Payment)
│   ├── 支付宝支付
│   ├── 微信支付
│   └── 回调处理
├── 用户模块 (Users)
│   ├── 用户管理
│   ├── 观看记录
│   └── 笔记管理
└── 管理模块 (Admin)
    ├── 数据统计
    ├── 视频管理
    └── 系统设置
```

---

## 三、核心技术设计

### 3.1 JWT 认证机制

**认证流程**：

1. 用户输入用户名/密码提交登录请求
2. 后端验证用户信息，使用 BCrypt 校验密码
3. 验证通过后生成 JWT Token（包含 userId、username、role）
4. 前端存储 Token 到 localStorage
5. 后续请求携带 Token（Authorization: Bearer xxx）
6. JwtAuthenticationFilter 拦截请求，验证 Token 有效性
7. 检查用户状态是否被禁用
8. 验证通过后设置 Spring Security 上下文

**Token 包含的信息**：
- `userId` - 用户ID
- `username` - 用户名
- `role` - 用户角色 (user/admin/super_admin)

**代码实现逻辑**（JwtAuthenticationFilter.java）：

```java
// 1. 从请求头获取JWT
String jwt = getJwtFromRequest(request);

// 2. 验证Token有效性
if (StringUtils.hasText(jwt) && jwtUtil.validateToken(jwt)) {
    // 3. 解析Token获取用户信息
    Claims claims = jwtUtil.parseToken(jwt);
    Integer userId = (Integer) claims.get("userId");
    String role = (String) claims.get("role");
    
    // 4. 检查用户状态
    Users user = usersService.getById(userId);
    if (user == null || !"active".equals(user.getStatus())) {
        // 用户禁用，返回403
        response.setStatus(HttpServletResponse.SC_FORBIDDEN);
        return;
    }
    
    // 5. 设置Spring Security上下文
    List<SimpleGrantedAuthority> authorities = new ArrayList<>();
    authorities.add(new SimpleGrantedAuthority("ROLE_" + role.toUpperCase()));
    UsernamePasswordAuthenticationToken authentication =
        new UsernamePasswordAuthenticationToken(username, null, authorities);
    SecurityContextHolder.getContext().setAuthentication(authentication);
}
```

---

### 3.2 角色权限控制 (RBAC)

**AOP 切面实现**（RoleCheckAspect.java）：

使用自定义 `@RequiresRoles` 注解实现方法级别的权限控制

```java
@Aspect
@Component
public class RoleCheckAspect {
    
    @Around("@annotation(com.example.demo.security.RequiresRoles)")
    public Object checkRole(ProceedingJoinPoint joinPoint) throws Throwable {
        // 获取当前用户角色
        String role = getCurrentUserRole(request);
        
        if (role == null) {
            throw new UnauthorizedException("未登录或登录已过期");
        }
        
        // 获取方法上的注解
        RequiresRoles requiresRoles = method.getAnnotation(RequiresRoles.class);
        
        // OR逻辑：用户拥有任一所需角色即可
        boolean hasPermission = Arrays.stream(requiredRoles)
            .anyMatch(requiredRole -> requiredRole.equals(role));
        
        if (!hasPermission) {
            throw new UnauthorizedException();
        }
        
        return joinPoint.proceed();
    }
}
```

**使用示例**：

```java
@RestController
@RequestMapping("/api/admin")
@RequiresRoles({"admin", "super_admin"})  // 类级别注解
public class AdminController {
    
    @GetMapping("/dashboard")
    public Result<Map<String, Object>> getDashboardStats() { ... }
    
    @PostMapping("/cache/clear")
    @RequiresRoles("super_admin")  // 方法级别注解（更严格）
    public Result<String> clearCache() { ... }
}
```

**角色说明**：

| 角色 | 说明 | 权限范围 |
|------|------|---------|
| `user` | 普通用户 | 观看公开视频、注册、登录 |
| `member` | 会员用户 | 观看所有视频、下载资料 |
| `admin` | 管理员 | 用户管理、基础数据统计 |
| `super_admin` | 超级管理员 | 系统管理、缓存清理、备份 |

---

### 3.3 会员权限校验

**核心逻辑**（UserServiceImpl.java）：

```java
public boolean isValidMember(Users user) {
    if (user == null) return false;
    
    // 管理员始终可访问
    if ("admin".equals(user.getRole()) || "super_admin".equals(user.getRole())) {
        return true;
    }
    
    // 会员角色需检查到期时间
    if ("member".equals(user.getRole())) {
        if (user.getMembershipExpireDate() == null) {
            return false;
        }
        return user.getMembershipExpireDate().after(new Date());
    }
    
    return false;
}
```

**视频访问控制**（VideosController.java）：

```java
@GetMapping("/{id}")
public ApiResponse<Videos> getById(@PathVariable Integer id, 
                                   @RequestHeader Authorization) {
    Videos video = videosService.getById(id);
    
    // 公开视频直接返回
    if (!"internal".equals(video.getAccessType())) {
        return ApiResponse.success(video);
    }
    
    // 会员视频，校验权限
    if (authHeader == null || !authHeader.startsWith("Bearer ")) {
        return ApiResponse.unauthorized("请先登录");
    }
    
    Users user = usersService.getById(userId);
    boolean hasAccess = userService.isValidMember(user);
    
    if (!hasAccess) {
        return ApiResponse.forbidden("仅会员可观看该视频，请先开通会员");
    }
    
    return ApiResponse.success(video);
}
```

---

### 3.4 第三方支付集成

**支付流程**：

1. 用户选择套餐（月度/季度/年度）
2. 前端调用后端创建支付订单接口
3. 后端生成外部订单号，创建待支付记录
4. 调用支付宝/微信支付API获取二维码链接
5. 使用ZXing生成二维码图片（Base64编码）
6. 返回二维码给前端展示
7. 用户扫码支付
8. 第三方支付平台回调通知
9. 后端验证签名，更新订单状态
10. 更新用户会员信息（同一事务）

**核心代码**（PaymentServiceImpl.java）：

```java
@Transactional  // 事务保证数据一致性
public PaymentResponse createAlipayPayment(PaymentRequest request) {
    // 1. 生成外部订单号
    String outTradeNo = generateTradeNo();
    
    // 2. 创建支付记录（待支付状态）
    Payment payment = new Payment();
    payment.setStatus(PaymentStatus.PENDING);
    payment.setExpireTime(LocalDateTime.now().plusMinutes(30));
    payment = paymentRepository.save(payment);
    
    // 3. 调用支付宝当面付API生成二维码
    AlipayTradePrecreateRequest alipayRequest = new AlipayTradePrecreateRequest();
    alipayRequest.setNotifyUrl("/api/payments/alipay/notify");
    Map<String, String> bizParams = new HashMap<>();
    bizParams.put("out_trade_no", outTradeNo);
    bizParams.put("total_amount", request.getAmount().toString());
    alipayRequest.setBizContent(JSON.toJSONString(bizParams));
    
    // 4. 获取二维码链接
    String qrCodeContent = response.getQrCode();
    
    // 5. 生成二维码图片（Base64）
    String qrCodeUrl = generateQrCode(qrCodeContent);
    
    return PaymentResponse.builder().qrCodeUrl(qrCodeUrl).build();
}

// 支付回调处理
@Transactional
public String handleAlipayCallback(Map<String, String> params) {
    // 1. 验证签名
    if (!verifyAlipaySign(params)) {
        return "failure";
    }
    
    // 2. 更新订单状态
    payment.setStatus(PaymentStatus.SUCCESS);
    paymentRepository.save(payment);
    
    // 3. 更新用户会员信息（同一事务）
    updateUserMembership(payment);
    
    return "success";
}
```

**会员信息更新**：

```java
@Transactional
public void updateUserMembership(Payment payment) {
    // 根据会员类型设置有效期
    switch (payment.getMembershipType()) {
        case YEARLY:   expireTime = LocalDateTime.now().plusYears(1); break;
        case QUARTERLY: expireTime = LocalDateTime.now().plusMonths(3); break;
        case MONTHLY:  expireTime = LocalDateTime.now().plusMonths(1); break;
    }
    
    // 更新用户角色：user → member
    Users user = usersService.getById(payment.getUserId());
    if ("user".equals(user.getRole())) {
        user.setRole("member");
    }
    user.setMembershipType(membershipType);
    user.setMembershipExpireDate(expireTime);
    usersService.updateById(user);
}
```

---

### 3.5 前端状态管理

**AuthContext 实现**（AuthContext.tsx）：

使用 React Context 实现全局认证状态管理

```typescript
export const AuthProvider = ({ children }: { children: ReactNode }) => {
  const [user, setUser] = useState<User | null>(null);
  const [isLoading, setIsLoading] = useState(true);

  // 初始化：从localStorage恢复登录状态
  useEffect(() => {
    const token = localStorage.getItem("token");
    const userInfo = localStorage.getItem("userInfo");
    if (token && userInfo) {
      setUser(JSON.parse(userInfo));
      setIsAuthenticated(true);
    }
  }, []);

  // 登录：保存token和用户信息
  const login = (token: string, userData: User) => {
    localStorage.setItem("token", token);
    localStorage.setItem("userInfo", JSON.stringify(userData));
    setUser(userData);
    setIsAuthenticated(true);
  };

  // 登出：清除所有登录信息
  const logout = () => {
    localStorage.removeItem("token");
    localStorage.removeItem("userInfo");
    setUser(null);
    router.push("/login");
  };

  // 刷新会员状态（支付后调用）
  const refreshMembership = async () => {
    const response = await membershipApi.getMembershipInfo();
    setUser({ ...user, ...response.data.data });
  };
};
```

---

## 四、数据库设计

### 4.1 核心数据表

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `users` | 用户表 | id, name, email, password, role, member_type, member_expire |
| `videos` | 视频表 | id, title, category_id, instructor_id, access_type, views |
| `payments` | 支付记录表 | id, user_id, amount, status, payment_type, out_trade_no |
| `watch_history` | 观看记录表 | id, user_id, video_id, progress, completed |
| `subscriptions` | 订阅表 | id, user_id, plan_type, start_date, end_date, status |
| `comments` | 评论表 | id, user_id, video_id, parent_id, content, status |

### 4.2 数据表关系

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    users    │       │   videos    │       │  payments   │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │       │ id (PK)     │       │ id (PK)     │
│ name        │       │ title       │       │ user_id(FK) │───┐
│ email       │       │ category_id │───────│ amount      │   │
│ password    │       │ instructor_id    │   │ status      │   │
│ role        │       │ access_type │       │ payment_type│   │
│ member_type │       │ views       │       │ out_trade_no│   │
│ member_expire│      └─────────────┘       └─────────────┘   │
└─────────────┘                                                         
      │                                                                
      │ 1:N        ┌─────────────┐       ┌─────────────┐               
      └───────────►│watch_history│       │ subscriptions│◄──────────────┘
                    ├─────────────┤       ├─────────────┤
                    │ id (PK)     │       │ user_id(FK) │
                    │ user_id(FK) │───────│ plan_type   │
                    │ video_id(FK)│───┐   │ start_date  │
                    │ progress    │   │   │ end_date    │
                    │ completed   │   │   └─────────────┘
                    └─────────────┘   │
                                    ▼
                            ┌─────────────────┐
                            │video_categories │
                            ├─────────────────┤
                            │ id (PK)         │
                            │ name            │
                            │ parent_id       │
                            └─────────────────┘
```

---

## 五、关键业务流程

### 5.1 用户注册流程

```
┌──────────────────────────────────────────────────────────────┐
│                     用户注册流程                              │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────┐
              │  填写注册信息    │
              │  (用户名/密码/邮箱)│
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  参数校验        │
              │  • 邮箱唯一性    │
              │  • 密码强度      │
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │  BCrypt加密密码   │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  保存用户到数据库 │
              │  • 角色=user     │
              │  • 状态=active   │
              │  • 会员编号生成  │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  生成JWT Token   │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  返回Token给前端  │
              └──────────────────┘
```

### 5.2 会员开通流程

```
┌──────────────────────────────────────────────────────────────┐
│                     会员开通流程                              │
└──────────────────────────────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────┐
              │  选择套餐        │
              │  (月/季/年)      │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  创建支付订单    │
              │  • 状态=PENDING  │
              │  • 30分钟有效期  │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  生成支付二维码   │
              │  (支付宝/微信)    │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  用户扫码支付     │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  第三方回调通知   │
              │  验证签名+更新状态│
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │  更新用户会员信息 │
              │  • role=user→member│
              │  • 设置到期时间   │
              └──────────────────┘
```

---

## 六、答辩重点问题准备

### 问题1：系统如何保证用户数据安全？

**回答要点**：
1. **密码安全**：使用 BCrypt 加密存储密码
2. **JWT Token**：短期有效，不可伪造
3. **用户状态**：禁用用户即使有Token也无法访问
4. **角色权限**：AOP切面精确控制每个接口的访问权限

```java
// 密码加密存储
user.setPassword(passwordUtil.encode(password));

// 禁用用户无法登录
if (user == null || !"active".equals(user.getStatus())) {
    return 403 Forbidden;
}
```

### 问题2：会员视频的访问控制如何实现？

**回答要点**：
1. **视频分类**：external（公开）/ internal（会员专享）
2. **双重校验**：登录状态 + 会员有效性
3. **管理员豁免**：admin/super_admin 自动放行

```java
// VideosController.java
if ("internal".equals(video.getAccessType())) {
    // 检查是否登录
    if (authHeader == null) return unauthorized;
    
    // 检查会员状态
    boolean hasAccess = userService.isValidMember(user);
    if (!hasAccess) return forbidden;
}
```

### 问题3：支付模块如何保证数据一致性？

**回答要点**：
1. **事务管理**：`@Transactional` 保证原子性
2. **状态机**：PENDING → PROCESSING → SUCCESS/FAILED
3. **幂等性**：外部订单号唯一约束
4. **签名验证**：回调必须验证签名

```java
@Transactional
public void handleAlipayCallback(Map<String, String> params) {
    // 1. 验证签名
    if (!verifyAlipaySign(params)) return "failure";
    
    // 2. 更新订单
    payment.setStatus(PaymentStatus.SUCCESS);
    paymentRepository.save(payment);
    
    // 3. 更新会员（同一事务）
    updateUserMembership(payment);
}
```

### 问题4：系统的技术亮点是什么？

**回答要点**：
1. **前后端分离架构**：Next.js + Spring Boot
2. **JWT无状态认证**：适合分布式部署
3. **AOP权限控制**：灵活、可扩展
4. **第三方支付集成**：支付宝+微信双通道
5. **事务一致性**：支付回调的原子性处理

### 问题5：为什么选择这些技术栈？

**回答要点**：
- **Spring Boot**：简化配置，内置Tomcat，开发效率高
- **MyBatis-Plus**：简化CRUD操作，支持分页插件
- **Next.js**：React框架，服务端渲染，SEO友好
- **Tailwind CSS**：原子化CSS，快速开发
- **JWT**：无状态认证，适合RESTful API

---

## 七、项目目录结构

```
在线课程项目/
├── zina-tch/                      # Spring Boot 后端
│   ├── src/main/java/com/example/demo/
│   │   ├── controller/            # 控制器层
│   │   │   ├── AuthController.java       # 认证控制器
│   │   │   ├── VideosController.java     # 视频控制器
│   │   │   ├── PaymentController.java    # 支付控制器
│   │   │   ├── AdminController.java     # 管理控制器
│   │   │   └── MembershipController.java # 会员控制器
│   │   ├── service/               # 服务层
│   │   │   └── impl/
│   │   │       ├── PaymentServiceImpl.java  # 支付服务实现
│   │   │       └── UserServiceImpl.java     # 用户服务实现
│   │   ├── security/             # 安全模块
│   │   │   ├── JwtAuthenticationFilter.java # JWT过滤器
│   │   │   ├── RoleCheckAspect.java         # AOP权限切面
│   │   │   └── RequiresRoles.java            # 权限注解
│   │   ├── pojo/                 # 实体类
│   │   │   ├── Users.java
│   │   │   ├── Videos.java
│   │   │   └── Payment.java
│   │   └── config/               # 配置类
│   │       └── SecurityConfig.java
│   └── pom.xml
│
├── medical-training/              # Next.js 前端
│   ├── app/                     # 页面
│   │   ├── page.tsx             # 首页
│   │   ├── login/page.tsx       # 登录页
│   │   ├── register/page.tsx    # 注册页
│   │   ├── courses/page.tsx     # 课程列表
│   │   ├── video/[id]/page.tsx # 视频播放页
│   │   ├── payment/page.tsx     # 支付页
│   │   └── admin/               # 管理后台
│   │       ├── page.tsx         # 管理首页
│   │       ├── videos/          # 视频管理
│   │       └── users/           # 用户管理
│   ├── components/              # 组件
│   │   ├── video-player.tsx     # 视频播放器
│   │   ├── featured-videos.tsx  # 热门视频
│   │   ├── navbar.tsx           # 导航栏
│   │   └── ProtectedRoute.tsx   # 路由保护
│   ├── contexts/                # 上下文
│   │   └── AuthContext.tsx      # 认证状态管理
│   └── package.json
│
└── medical_training.sql          # 数据库脚本
```

---

## 八、测试用例汇总

### 8.1 管理员功能测试用例

| 模块 | 测试内容 | 预期结果 |
|------|----------|----------|
| 用户管理 | 获取用户列表 | 返回分页用户数据 |
| 用户管理 | 修改用户角色 | 角色成功更新 |
| 用户管理 | 删除用户 | 级联删除相关数据 |
| 视频管理 | 创建新视频 | 视频创建成功 |
| 视频管理 | 设置访问类型 | external/internal 正确设置 |
| 视频管理 | 删除视频 | 视频及文件删除 |
| 数据统计 | 获取后台统计 | 返回用户/会员数量 |

### 8.2 普通用户功能测试用例

| 模块 | 测试内容 | 预期结果 |
|------|----------|----------|
| 用户注册 | 正常注册 | 注册成功，返回token |
| 用户注册 | 邮箱已存在 | 返回错误提示 |
| 开通会员 | 选择套餐支付 | 生成支付二维码 |
| 开通会员 | 会员状态查询 | 返回会员信息 |
| 扫码支付 | 支付宝支付 | 生成支付宝二维码 |
| 扫码支付 | 支付回调 | 会员状态更新 |
| 观看记录 | 记录观看进度 | 进度自动保存 |
| 热门推荐 | 获取热门视频 | 返回热门课程列表 |

---

## 九、技术创新点

1. **无状态认证**：JWT Token 实现无状态认证，便于水平扩展
2. **AOP权限控制**：使用切面技术实现细粒度的权限控制
3. **事务一致性**：支付流程保证数据一致性
4. **组件化开发**：前端组件化，提高代码复用性
5. **响应式设计**：适配多终端设备

---

## 十、总结

本项目实现了一个完整的在线课程系统，包含以下核心功能：

- 用户认证与权限管理
- 视频课程管理
- 会员订阅与支付
- 观看记录与进度同步

系统采用前后端分离架构，后端使用 Spring Boot + MyBatis-Plus，前端使用 Next.js + TypeScript，通过 JWT 实现安全的无状态认证，通过 AOP 实现灵活的权限控制，通过事务管理保证支付流程的数据一致性。
